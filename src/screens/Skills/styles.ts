import styled from "styled-components"

export const ContainerSkills = styled.section`
    
`
export const Card = styled.div`
    border: 1px solid ${({ theme }) => theme.colors.dark_gold};
    padding: 10px;
    color: ${({ theme }) => theme.colors.dark_gold};
`